/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['example.com', 'images.pexels.com', 'wwww.globalgiving.org'], // Add all the domains you need
    },
  };
  
  export default nextConfig;
  